# CLAUDE.md

Read and follow `AGENTS.md`; it is the canonical workspace policy.

Work in exactly one active task. Use its `task.json` and `events.jsonl` when the
harness is available, never read unrelated task folders, and keep browser
action authority separate from external disclosure and web-chat advice.

Reasoning delegates may plan or review, but only the local agent may execute
authorized browser actions and verify their outcomes.
